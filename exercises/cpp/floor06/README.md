# C++ FLOOR 6

CLASSES & RAII — constructors, destructors, encapsulation, the RAII idea. Project: the Entity system.

Outline only. When you arrive, ask Claude (or Claude Code) to generate exercises + tests here following the C++ Floor 1-3 format: TODO .cpp files, devtest.h-based tests in tests/, ids (c6-1 …) wired into dojo.py MANIFEST.
